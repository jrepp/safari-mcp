node_modules/
.DS_Store
*.log
.npmrc

.mcpregistry_*
.launch-day/

# MCP Registry publisher binary (downloaded locally, not for commit)
mcp-publisher

# Safari helper binary (built/downloaded locally, not for commit)
safari-helper

# Smithery build artifacts
.smithery/
